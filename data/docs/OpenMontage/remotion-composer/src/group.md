# src

**Path:** `remotion-composer/src`

**Purpose**
==========

This group (`remotion-composer/src`) is responsible for providing utility functions to resolve and manipulate media metadata and theme resolution for Remotion.

**Key Components**
------------------

* **Theme Resolution**: `resolveTheme` function is a key component, responsible for resolving theme settings from various sources.
* **Color Utilities**: Functions like `toneGradient`, `isLightColor`, and `shiftColor` provide color manipulation capabilities.
* **Asset Management**: `resolveAsset` function is used to resolve assets, while `calculateMetadata` function calculates metadata for media.

**Dependencies**
---------------

This group likely depends on other components in the Remotion library, such as media processing and rendering functions.

**Invariants**
-------------

* The group assumes that theme settings are resolvable from a specific set of sources.
* Color utilities are designed to work with RGB color values.
* Asset resolution and metadata calculation functions rely on the presence of valid media inputs.

---

**Members:** 9 nodes, 0 specs

**Provenance:** Generated by Spec-Atlas at indexing time.