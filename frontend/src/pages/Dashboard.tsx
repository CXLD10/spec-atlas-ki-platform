export function Dashboard() {
  return (
    <div style={{ padding: '2rem' }}>
      <h1>Dashboard</h1>
      <p>Coming in Phase 1 — hero + omni-bar + sources grid + how-it-works + capabilities</p>
    </div>
  )
}

export default Dashboard
